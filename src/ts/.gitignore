node_modules/
dist/
# Generated by `just docs` (typedoc). Anchored so it never matches a docs/
# folder under src/ or test/. Not in package.json `files`, so it is never
# published to npm either.
/docs/
